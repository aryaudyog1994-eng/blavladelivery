Client (React + Vite)
Run:
1. cd client
2. npm install
3. npm run dev
Notes:
- By default client expects server at http://localhost:4000
- For production, host server and client together and update API base URLs
